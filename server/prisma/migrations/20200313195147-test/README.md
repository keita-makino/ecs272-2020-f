# Migration `20200313195147-test`

This migration has been generated by keita-makino at 3/13/2020, 7:51:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."RecordType" ALTER COLUMN "room" SET DEFAULT '0';

ALTER TABLE "public"."Setting" ADD COLUMN "height" Decimal(65,30)  NOT NULL DEFAULT 5;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200312223009-setting..20200313195147-test
--- datamodel.dml
+++ datamodel.dml
@@ -3,37 +3,38 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgres://ojqgzezcrkkccv:413545d8bc9cfbeb322834d87f2369199c51d226e374dc06da6883681e1fa266@ec2-35-172-85-250.compute-1.amazonaws.com:5432/d23tt9hq4p0gh8"
 }
 model Record {
   address String     @default("")
   id      Int        @default(autoincrement()) @id
   lat     Float      @default(0)
   lng     Float      @default(0)
   name    String     @default("")
-  room    Room
   type    RecordType
 }
 model RecordType {
-  id      Int      @id
-  name    String   @default("")
-  records Record[]
+  id     Int      @id
+  name   String   @default("")
+  room   Int
+  record Record[]
 }
 model Room {
-  id      Int      @default(autoincrement()) @id
-  name    String   @default("")
-  records Record[]
+  id   Int    @default(autoincrement()) @id
+  name String @default("")
 }
 model Setting {
-  cellSize Float @default(0.00025)
-  id       Int   @id
-  markSize Float @default(5)
+  cellSize Float   @default(0.00025)
+  darkMode Boolean @default(false)
+  id       Int     @id
+  markSize Float   @default(5)
+  height   Float   @default(5)
   user     User
 }
 model User {
```


